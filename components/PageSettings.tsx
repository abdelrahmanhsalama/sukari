import Card from "./Card";
import ParentCard from "./ParentCard";

const PageSettings = () => {
  return (
    <ParentCard>
      <Card>...</Card>
    </ParentCard>
  );
};

export default PageSettings;
